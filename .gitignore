# Secrets and local overrides
.env
.env.*
!.env.example
*.local
docker-compose.override.yml

# Editor / OS
.DS_Store
*.swp
*.swo
*~
*.bak
.idea/
.vscode/
*.code-workspace

# Logs, caches, Python
*.log
__pycache__/
*.pyc
.venv/

# Build / scratch
htdocs/assets/netping_latest.tar.gz
tmp/
*.rrd

# Addon reverse-proxy: the image's IncludeOptional target and the
# site-local file it chains to are host-specific, so only the .example
# is tracked (see conf/addons-proxy.conf.example).
conf/addons-proxy.conf
conf/addons-site.conf
